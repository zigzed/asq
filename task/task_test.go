package task

import (
	"testing"
)

func TestTaskCoded(t *testing.T) {
	// is := is.New(t)

	// t1 := NewTask(NewTaskOption(1, 15*time.Second), "test", 1, "a")
	// jm := marshaller.NewJsonMarshaller()
	// buf, err := jm.EncodeTask(t1)
	// is.NoErr(err)
	// fmt.Printf("%s\n", string(buf))

	// t2, err := jm.DecodeTask(buf)
	// is.NoErr(err)
	// is.NotNil(t2)

	// fmt.Printf("%+v\n", *t2)
}
